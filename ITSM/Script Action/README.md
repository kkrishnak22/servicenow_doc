**What is Script Action ?**

Script actions are server side script that
execute asynchronously when event is
triggered.
In other words we can say that when event is
processed than script action gets Triggered.
• We need event to trigger Script Action

we can access the parameters that we pass using eventqueue by using event.param1