when a table is extended in servicenow how many fields does the child table recevie (70 parent fields, so how many child table fields? (70 or 71))

when creating tables how to enable default acl creation and disable them? 
- throgh app engine the acls are direclty created

TODO: Need to deep down and write the answers