gs.eventQueue('event.name', GlideRecord, parm1, parm2,queue_name);

/**gs.eventQueue(
					'x_bgstm_prism.notify.sme', 
					current,                    
					user,   // sys_id of the user to notify                    
					fieldName // param2
				);
    */